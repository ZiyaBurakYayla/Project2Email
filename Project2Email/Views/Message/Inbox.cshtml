@model List<Project2Email.Dtos.InboxMessageDto>

@{
    ViewData["Title"] = "Gelen Kutusu";
    Layout = "~/Views/Layout/Index.cshtml";
}

<style>
    .email-card {
        background: var(--bg-paper);
        border-radius: 0.5rem;
        height: 100%;
        display: flex;
        flex-direction: column;
        border: 1px solid var(--border-color);
    }

    .email-toolbar {
        padding: 0.8rem 1.5rem;
        border-bottom: 1px solid var(--border-color);
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .toolbar-left, .toolbar-right {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .icon-btn {
        background: transparent;
        border: none;
        color: var(--text-secondary);
        font-size: 1.25rem;
        padding: 0.4rem;
        border-radius: 50%;
        transition: 0.2s;
    }

        .icon-btn:hover {
            background-color: var(--hover-bg);
            color: var(--text-primary);
        }

    .email-list-container {
        flex-grow: 1;
        overflow-y: auto;
    }

    .email-list-item {
        display: flex;
        align-items: center;
        padding: 1rem 1.5rem;
        border-bottom: 1px solid var(--border-color);
        cursor: pointer;
        transition: 0.2s;
    }

        .email-list-item:hover {
            background-color: rgba(255,255,255,0.02);
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
            z-index: 1;
            border-color: transparent;
        }

    .form-check-input {
        background-color: var(--bg-body);
        border-color: #6f7194;
        cursor: pointer;
    }

        .form-check-input:checked {
            background-color: #696cff;
            border-color: #696cff;
        }

    .email-content {
        flex-grow: 1;
        display: flex;
        align-items: center;
        overflow: hidden;
        white-space: nowrap;
        margin-left: 1rem;
    }

    .email-subject {
        font-weight: 600;
        color: var(--text-primary);
        margin-right: 0.5rem;
    }

    .email-snippet {
        color: var(--text-secondary);
        font-size: 0.9rem;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .email-meta {
        text-align: right;
        min-width: 120px;
        margin-left: 1rem;
    }

    .sender-name {
        font-weight: 600;
        font-size: 0.85rem;
        color: var(--text-primary);
        display: block;
    }

    .email-date {
        font-size: 0.75rem;
        color: #6f7194;
    }

    .star-icon {
        cursor: pointer;
        font-size: 1.4rem;
        color: #6f7194;
        transition: 0.2s;
        z-index: 10;
        position: relative;
    }

        .star-icon:hover {
            transform: scale(1.2);
        }

        .star-icon.starred {
            color: #ffab00;
        }
</style>

<div class="email-card">
    <div class="email-toolbar">
        <div class="toolbar-left">
            <div class="form-check me-2">
                <input class="form-check-input" type="checkbox" id="selectAll">
            </div>

            <button class="icon-btn" title="Yenile" onclick="location.reload()"><i class='bx bx-refresh'></i></button>

            <button class="icon-btn" title="Çöp Kutusuna Taşı" onclick="deleteSelected()">
                <i class='bx bx-trash'></i>
            </button>

            <button class="icon-btn" title="Okunma Durumunu Değiştir" onclick="toggleReadSelected()">
                <i class='bx bx-envelope'></i>
            </button>
        </div>

        <div class="toolbar-right text-muted small">
            <span style="color: var(--text-secondary);">@Model.Count</span>
            <button class="icon-btn"><i class='bx bx-chevron-left'></i></button>
            <button class="icon-btn"><i class='bx bx-chevron-right'></i></button>
        </div>
    </div>

    <div class="email-list-container">
        @foreach (var item in Model)
        {
            var starClass = item.IsStarred ? "bxs-star starred" : "bx-star";

            <div class="email-list-item @(item.IsRead ? "" : "fw-bold")" id="row-@item.MessageId">

                <input class="form-check-input msg-checkbox" type="checkbox" value="@item.MessageId" onclick="event.stopPropagation()">

                <i class='bx @starClass star-icon ms-3' onclick="toggleStar(event, this, @item.MessageId)"></i>

                <div class="email-content" onclick="goToDetail(@item.MessageId)">
                    <span class="email-subject">@item.Subject</span>
                    <span class="email-snippet"> - @item.Summary</span>
                </div>

                <div class="email-meta">
                    <span class="sender-name">@item.SenderEmail</span>
                    <span class="email-date">@item.SendDate.ToString("dd MMM")</span>
                </div>
            </div>
        }
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script>
    function goToDetail(id) {
        window.location.href = '/Message/Detail/' + id;
    }

    $('#selectAll').change(function() {
        var isChecked = $(this).prop('checked');
        $('.msg-checkbox').prop('checked', isChecked);
    });

    function deleteSelected() {
        var selectedIds = [];
        $('.msg-checkbox:checked').each(function() {
            selectedIds.push($(this).val());
        });

        if (selectedIds.length === 0) {
            Swal.fire({ icon: 'warning', title: 'Mesaj Seçmediniz', text: 'Lütfen silmek için en az bir mesaj seçin.', confirmButtonColor: '#696cff' });
            return;
        }

        Swal.fire({
            title: 'Emin misiniz?',
            text: selectedIds.length + " mesaj çöp kutusuna taşınacak.",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#696cff',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Evet, Sil',
            cancelButtonText: 'İptal'
        }).then((result) => {
            if (result.isConfirmed) {
                $.ajax({
                    type: "POST",
                    url: "/Message/MarkAsTrash",
                    data: { ids: selectedIds },
                    success: function(response) {
                        if (response.success) {
                            selectedIds.forEach(id => {
                                $('#row-' + id).fadeOut(300, function() { $(this).remove(); });
                            });
                            Swal.fire({ icon: 'success', title: 'Taşındı!', text: 'Mesajlar çöp kutusuna atıldı.', timer: 1500, showConfirmButton: false });
                        }
                    }
                });
            }
        });
    }

    function toggleReadSelected() {
        var selectedIds = [];
        $('.msg-checkbox:checked').each(function() {
            selectedIds.push($(this).val());
        });

        if (selectedIds.length === 0) {
            Swal.fire({ icon: 'warning', title: 'Mesaj Seçmediniz', text: 'Lütfen işlem yapmak için en az bir mesaj seçin.', confirmButtonColor: '#696cff' });
            return;
        }

        $.ajax({
            type: "POST",
            url: "/Message/ToggleReadStatus",
            data: { ids: selectedIds },
            success: function(response) {
                if (response.success) {
                    selectedIds.forEach(id => {
                        var row = $('#row-' + id);
                        if (row.hasClass('fw-bold')) {
                            row.removeClass('fw-bold');
                        } else {
                            row.addClass('fw-bold');
                        }
                    });

                    $('.msg-checkbox').prop('checked', false);
                    $('#selectAll').prop('checked', false);
                }
            }
        });
    }

    function toggleStar(e, element, messageId) {
        if(e) { e.stopPropagation(); }
        var icon = $(element);
        if (icon.hasClass('bxs-star')) {
             icon.removeClass('bxs-star starred').addClass('bx-star');
        } else {
             icon.removeClass('bx-star').addClass('bxs-star starred');
        }
        $.ajax({
            type: "POST",
            url: "/Message/ToggleStar",
            data: { id: messageId },
            success: function (response) {
                if (response.success) {
                    if (response.isStarred) icon.removeClass('bx-star').addClass('bxs-star starred');
                    else icon.removeClass('bxs-star starred').addClass('bx-star');
                } else {
                    location.reload();
                }
            }
        });
    }
</script>